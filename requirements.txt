# Requires Python 3.10+ (uses PEP 604 union syntax, e.g. `str | None`,
# without `from __future__ import annotations`).
#
# Local model inference uses Ollama, a separate application, not a Python
# package -- see README for install instructions.
#
# psycopg below is needed only by the separate Vercel-hosted demo
# (api/db_pg.py), which persists to Postgres instead of a local SQLite
# file a stateless serverless function can't durably write to. Vercel's
# non-framework /api build only reads one project-root requirements.txt,
# not a per-function one, which is why it's listed here rather than
# scoped to api/ -- never imported by src/, tests/, or extras/.
psycopg[binary]==3.2.3

# document_qa.py's chat-upload feature: pypdf reads a PDF's real text
# layer (a required dependency for that feature -- it's the only way it
# reads a PDF at all). pytesseract is the Python side of image OCR, but
# needs the separate Tesseract engine installed to actually do anything
# (see README) -- exactly the same kind of optional local dependency
# Ollama already is for the LLM arbiter; without it, document_qa.py
# degrades to an honest "OCR isn't available" instead of failing.
pypdf==6.13.1
pytesseract==0.3.13
Pillow>=10.0
